<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@android:color/white">

    <!-- Token Balances Section Header -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Token Balances"
        android:textColor="@color/text_secondary"
        android:textSize="14sp"
        android:layout_gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Container for token balance rows -->
    <LinearLayout
        android:id="@+id/tokenBalancesContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" />

    <!-- Manage Tokens Button -->
    <Button
        android:id="@+id/manage_viewing_keys_button"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:text="Manage Tokens"
        android:textSize="12sp"
        android:textColor="#22C55E"
        android:textAllCaps="false"
        android:background="@drawable/btn_outline_green"
        app:backgroundTint="@null"
        android:elevation="0dp"
        android:stateListAnimator="@null"
        android:paddingHorizontal="16dp"
        android:layout_gravity="end"
        android:layout_marginTop="12dp" />

</LinearLayout>
