<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.passportscanner">

    <!-- NFC Permissions -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true" />

    <!-- Internet Permission for fetching CRLs and other data -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Camera Permission for capturing passport photo -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    
    <!-- Notification Permission for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.PassportScanner"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        
        <activity
            android:name=".HostActivity"
            android:exported="true"
            android:theme="@style/Theme.PassportScanner"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MRZInputActivity"
            android:exported="true"
            android:theme="@style/Theme.PassportScanner"
            android:screenOrientation="portrait" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PassportScanner"
            android:screenOrientation="portrait">
            <!-- Intent filter for NFC tag discovery -->
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
            </intent-filter>
            
            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />
        </activity>

        <!-- Secret Wallet Activity -->
        <activity
            android:name=".wallet.WalletActivity"
            android:exported="false"
            android:theme="@style/Theme.PassportScanner"
            android:screenOrientation="portrait" />
        
        <!-- Create Wallet Activity (Keplr-style flow) -->
        <activity
            android:name=".wallet.CreateWalletActivity"
            android:exported="false"
            android:theme="@style/Theme.PassportScanner"
            android:screenOrientation="portrait" />
        
        <!-- Wallet list activity (manage multiple wallets) -->
        <activity
            android:name=".wallet.WalletListActivity"
            android:exported="false"
            android:theme="@style/Theme.PassportScanner"
            android:screenOrientation="portrait" />

        <!-- ANML Claim Activity -->
        <activity
            android:name=".ANMLClaimActivity"
            android:exported="false"
            android:theme="@style/Theme.PassportScanner"
            android:screenOrientation="portrait" />

        <!-- Actions Navigator Activity -->
        <activity
            android:name=".ActionsActivity"
            android:exported="false"
            android:theme="@style/Theme.PassportScanner"
            android:screenOrientation="portrait" />
        
        <!-- Secret Query Activity (headless, invisible WebView) -->
        <activity
            android:name=".bridge.SecretQueryActivity"
            android:exported="false"
            android:theme="@style/Theme.PassportScanner.NoDisplay"
            android:excludeFromRecents="true" />
        
        <!-- Secret Signer Activity (headless, on-device signing) -->
        <activity
            android:name=".bridge.SecretSignerActivity"
            android:exported="false"
            android:theme="@style/Theme.PassportScanner"
            android:excludeFromRecents="true" />
        
        <!-- Secret Execute Activity (headless, invisible WebView signer) -->
        <activity
            android:name=".bridge.SecretExecuteActivity"
            android:exported="false"
            android:theme="@style/Theme.PassportScanner.NoDisplay"
            android:excludeFromRecents="true" />

        <!-- Secret Execute Native Activity (headless, pure-native signer/broadcaster) -->
        <activity
            android:name=".bridge.SecretExecuteNativeActivity"
            android:exported="false"
            android:theme="@style/Theme.PassportScanner.NoDisplay"
            android:excludeFromRecents="true" />
    </application>

</manifest>