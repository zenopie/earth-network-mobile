<?xml version="1.0" encoding="utf-8"?>
<!-- Wallet list layout without bottom navigation - relies on host activity navigation -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/wallet_list_root"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Scrollable area — now uses full height -->
    <ScrollView
        android:id="@+id/wallet_list_scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:padding="16dp">

        <LinearLayout
            android:id="@+id/wallet_list_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingBottom="8dp">

                <TextView
                    android:id="@+id/wallet_list_title"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Wallets"
                    android:textSize="20sp"
                    android:textStyle="bold" />

                <Button
                    android:id="@+id/btn_add_wallet_list"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Add Wallet" />
            </LinearLayout>

            <!-- Rows will be added programmatically into this container -->
        </LinearLayout>
    </ScrollView>

</LinearLayout>